#include <bits/stdc++.h>
using namespace std;

string EvenOrOdd (int n) {
    return (n & 1) ? "Odd" : "Even";
}

int main() {
    // cout << EvenOrOdd(1) << ' ' << EvenOrOdd(2) << endl;
    return 0;
}